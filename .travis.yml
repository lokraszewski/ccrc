dist: trusty
sudo: required
language:
  - cpp
compiler:
  - clang
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - clang-5.0
      - cmake
script:
  - mkdir build && cd build
  - cmake ..
  - make
